# NOTICE / 边界声明

## 1. 本仓库包含什么

* **XLMod 团队自行编写的全部代码**：`mod-src/**`（Java）、`guard-src/**`（由 `guard_gen.py` 生成）、
  构建与验证脚本（`*.py`）、混淆规则（`*.pro`）、远程配置（`features.json`）、设计文档（`docs/**`）。

## 2. 本仓库不包含什么（因版权原因）

* 宿主 App（学乐云 `net.xuele.xuelets`）的任何原始代码、资源、图标、字符串；
* 对宿主 App 反编译后 smali 的**修改补丁**；
* 任何 APK / DEX / 反编译产物（见 `.gitignore`）。

我们只提供**注入点清单**（`docs/INJECTION_POINTS.md`）：说明"在哪个类的哪个方法、什么时机、
调用本仓库的哪个公开接口"，不含原版代码，也不足以据此还原原版实现。

## 3. 与宿主 App 的关系

* 本项目是**独立编写的增强模块**，通过反射与公开接口与宿主 App 交互；
* 宿主 App 的商标、版权归其权利人所有；
* 使用者需自行评估当地法律与平台条款，风险自负。

## 4. 许可证

本仓库全部内容以 **AGPL-3.0**（`LICENSE`）发布。要点：

* 允许使用、修改、再分发；
* **通过网络向他人提供服务时，必须向使用者提供完整对应源码**（AGPL 第 13 条）；
* 分发修改版时须保留版权声明与许可证，并标明修改；
* 若你无法接受 AGPL 条款，请不要使用本仓库代码。

## 5. 第三方

* `r8-8.3.37.jar`、Android SDK build-tools、JDK、Python 及其依赖均为各自权利人所有，
  不属于本仓库内容，也不随仓库分发（构建时自行准备）。
